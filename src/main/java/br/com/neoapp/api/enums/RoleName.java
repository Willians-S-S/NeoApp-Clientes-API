package br.com.neoapp.api.enums;

/**
 * Enum que define os papéis (roles) de autorização disponíveis no sistema.
 * Utilizado para controlar o acesso a diferentes recursos da API.
 */
public enum RoleName {
    ADMIN,
    USER
}
